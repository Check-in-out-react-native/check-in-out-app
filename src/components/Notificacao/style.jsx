import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    banner: {
      position: 'absolute',
      top: 0,
      left: 0,
      right: 0,
      zIndex: 9999
    }
});