import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
      minWidth:'330px',
      margin:'auto',
      display:'flex',
    },
    texto: {
        fontSize:24,
        fontFamily:'Tahoma',
    }
  });