import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    avatar: {
        backgroundColor: null,
        fontSize: 10
    },
    surface: {
      display: 'flex', 
      flexDirection: 'row', 
      alignItems: 'center'
    }
});